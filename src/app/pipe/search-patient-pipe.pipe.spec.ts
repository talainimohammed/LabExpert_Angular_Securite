import { SearchPatientPipe } from './search-patient.pipe';

describe('SearchPatientPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPatientPipe();
    expect(pipe).toBeTruthy();
  });
});
