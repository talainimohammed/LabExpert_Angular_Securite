import { SearchEchantillonPipe } from './search-echantillon.pipe';

describe('SearchEchantillonPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEchantillonPipe();
    expect(pipe).toBeTruthy();
  });
});
